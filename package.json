{
  "name": "jwst-gumloop",
  "version": "1.0.0",
  "private": true,
  "description": "A data-analyst agent on Gumloop over Snowflake, with a regression gate on prompt changes.",
  "type": "module",
  "scripts": {
    "test": "tsx --test evals/scorers.test.ts",
    "typecheck": "tsc --noEmit",
    "build:warehouse": "python3 data/build_warehouse.py && python3 data/verify_sql.py",
    "experiment": "tsx evals/run_experiment.ts",
    "gate": "tsx evals/gate.ts",
    "fixtures": "tsx evals/make_fixtures.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
